- if Rails.env.production?
  :javascript
    (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
    (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
    m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
    })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

    ga('create', '#{configus.counters.google.id}', 'auto');
    ga('send', 'pageview');

    var starttime = Math.round((new Date()).getTime() / 1000);
    window.onbeforeunload = function() {
    var difftime = Math.round(((new Date()).getTime()*0.001 - (starttime))/60);
    if (difftime) { ga('send', 'event', 'exit', difftime+" min"); }; // ping GA on exit
    };

    setInterval( function() { ga('send', 'event', 'watch', Math.round( ((new Date()).getTime()*0.001 - (starttime))/60) + " min"); } , 300000);

  / Yandex.Metrika counter
  = metrica_init
  / /Yandex.Metrika counter
